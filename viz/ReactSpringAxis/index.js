import ReactDOM from "react-dom";
import { ReactSpringAxis } from "./ReactSpringAxis";

const rootElement = document.getElementById("root");
ReactDOM.render(<ReactSpringAxis width={800} height={300} />, rootElement);
