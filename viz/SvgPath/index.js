// File used to render something in codesandbox only
import ReactDOM from 'react-dom';
import { SvgPath } from './SvgPath';

const rootElement = document.getElementById('root');
ReactDOM.render(<SvgPath />, rootElement);
