import { HistogramDatasetTransition } from "./HistogramDatasetTransition";

export const HistogramDatasetTransitionDemo = ({
  width = 700,
  height = 400,
}) => <HistogramDatasetTransition width={width} height={height} />;
