import ReactDOM from 'react-dom';
import { MapPlayground } from './MapPlayground';

const rootElement = document.getElementById('root');
ReactDOM.render(<MapPlayground width={width} height={height} />, rootElement);
