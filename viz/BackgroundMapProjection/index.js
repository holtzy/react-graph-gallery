import ReactDOM from 'react-dom';
import { MapPlayground } from './MapPlayground';

const rootElement = document.getElementById('root');
ReactDOM.render(<MapPlayground width={600} height={600} />, rootElement);
