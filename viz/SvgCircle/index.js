// File used to render something in codesandbox only
import ReactDOM from 'react-dom';
import { SvgCircle } from './SvgCircle';

const rootElement = document.getElementById('root');
ReactDOM.render(<SvgCircle />, rootElement);
