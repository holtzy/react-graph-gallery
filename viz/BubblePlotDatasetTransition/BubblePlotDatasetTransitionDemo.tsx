import { BubblePlotDatasetTransition } from "./BubblePlotDatasetTransition";

export const BubblePlotDatasetTransitionDemo = ({
  width = 700,
  height = 400,
}) => <BubblePlotDatasetTransition width={width} height={height} />;
