import { ReactSpringDerivingValue } from './ReactSpringDerivingValue';

export const ReactSpringDerivingValueDemo = ({ width = 700, height = 400 }) => (
  <ReactSpringDerivingValue width={width} height={height} />
);
