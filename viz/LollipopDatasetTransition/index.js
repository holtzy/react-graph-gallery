import ReactDOM from "react-dom";
import { LollipopDatasetTransition } from "./LollipopDatasetTransition";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <LollipopDatasetTransition width={800} height={300} />,
  rootElement
);
