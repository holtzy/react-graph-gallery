import { RadarDatasetAnimation } from './RadarDatasetAnimation';

export const RadarDatasetAnimationDemo = ({ width = 700, height = 400 }) => {
  return <RadarDatasetAnimation width={width} height={height} />;
};
