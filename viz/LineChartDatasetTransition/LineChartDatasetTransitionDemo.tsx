import { LineChartDatasetTransition } from "./LineChartDatasetTransition";

export const LineChartDatasetTransitionDemo = ({
  width = 700,
  height = 400,
}) => <LineChartDatasetTransition width={width} height={height} />;
