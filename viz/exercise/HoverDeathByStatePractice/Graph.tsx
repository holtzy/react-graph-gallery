import { scaleLinear } from 'd3';

const MARGIN = {};

const width = 500;
const height = 300;

export const Graph = () => {
  const boundsWidth = '';
  const boundsHeight = '';

  const xScale = 'TODO';
  const yScale = 'TODO';

  return (
    <svg width={500} height={300}>
      {/* SVG background */}
      <rect />

      {/* Bounds area */}
      <g>
        <rect fill="lightgrey" />
        <circle />
        <circle />
      </g>
    </svg>
  );
};
