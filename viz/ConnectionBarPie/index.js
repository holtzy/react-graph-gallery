import ReactDOM from 'react-dom';
import { ConnectionBarPie } from './ConnectionBarPie';

const rootElement = document.getElementById('root');
ReactDOM.render(<ConnectionBarPie width={600} height={400} />, rootElement);
