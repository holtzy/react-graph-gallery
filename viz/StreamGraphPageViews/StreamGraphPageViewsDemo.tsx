import { StreamGraphPageViews } from "./StreamGraphPageViews";

export const StreamGraphPageViewsDemo = ({ width = 700, height = 400 }) => (
  <StreamGraphPageViews width={width} height={height} />
);
