import { DonutBarplotTransition } from "./DonutBarplotTransition";

export const DonutBarplotTransitionDemo = ({ width = 700, height = 400 }) => (
  <DonutBarplotTransition width={width} height={height} />
);
